{
  "name": "ams-worker",
  "private": true,
  "version": "0.0.1",
  "description": "AMS \u2014 token stream routing. Day 1 PoC slice (control plane only).",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.test.json",
    "test": "tsx --test src/bootstrap.test.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250501.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.80.0",
    "tsx": "^4.19.0",
    "@types/node": "^20.17.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "agents": "^0.12.3",
    "zod": "^4.4.3"
  }
}