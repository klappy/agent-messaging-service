{
  "name": "agent-runtime",
  "private": true,
  "version": "0.0.1",
  "description": "Persona-shaped agent runtime per klappy://canon/methods/persona-shaped-agent-runtime — vodka-thin layer that resolves persona profiles, composes system prompts, dispatches to a substrate, and post-processes outputs by surface. Substrate-agnostic; v1 ships with the Anthropic Messages API path only.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250501.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.80.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.96.0",
    "yaml": "^2.9.0"
  }
}
